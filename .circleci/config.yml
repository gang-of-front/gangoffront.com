version: '2.1'
working_directory: &working_directory ~/gangoffront

orbs:
  terraform: circleci/terraform@3.1

workflows:
  deploy_infrastructure:
    jobs:
      - terraform/fmt:
          checkout: true
          context: terraform
          path: ~/gangoffront/terraform
      - terraform/validate:
          checkout: true
          context: terraform
          path: ~/gangoffront/terraform
          requires:
            - terraform/fmt
      - terraform/plan:
          checkout: true
          context: terraform
          path: ~/gangoffront/terraform
          persist-workspace: true
          requires:
            - terraform/validate
      - approval:
          type: approval
          requires:
            - terraform/plan
      - terraform/apply:
          attach-workspace: true
          context: terraform
          path: ~/gangoffront/terraform
          filters:
            branches:
              only: main
          requires:
            - approval
